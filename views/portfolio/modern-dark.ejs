<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= portfolio.title %></title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    <% if (portfolio.colorScheme === 'dark') { %>
    :root {
      --primary-color: #3498db;
      --bg-color: #121212;
      --text-color: #f5f5f5;
      --card-bg: #1e1e1e;
      --border-color: #333;
    }
    <% } else if (portfolio.colorScheme === 'blue') { %>
    :root {
      --primary-color: #2980b9;
      --bg-color: #1a2a3a;
      --text-color: #f5f5f5;
      --card-bg: #2c3e50;
      --border-color: #34495e;
    }
    <% } else if (portfolio.colorScheme === 'green') { %>
    :root {
      --primary-color: #27ae60;
      --bg-color: #0a2e1c;
      --text-color: #f5f5f5;
      --card-bg: #1e3d2f;
      --border-color: #2ecc71;
    }
    <% } else if (portfolio.colorScheme === 'purple') { %>
    :root {
      --primary-color: #8e44ad;
      --bg-color: #2c0a31;
      --text-color: #f5f5f5;
      --card-bg: #3c1642;
      --border-color: #9b59b6;
    }
    <% } else if (portfolio.colorScheme === 'light') { %>
    :root {
      --primary-color: #3498db;
      --bg-color: #f5f5f5;
      --text-color: #333;
      --card-bg: #fff;
      --border-color: #ddd;
    }
    <% } else { %>
    :root {
      --primary-color: #3498db;
      --bg-color: #121212;
      --text-color: #f5f5f5;
      --card-bg: #1e1e1e;
      --border-color: #333;
    }
    <% } %>
    
    body {
      background-color: var(--bg-color);
      color: var(--text-color);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    
    .navbar {
      background-color: rgba(0, 0, 0, 0.8);
      backdrop-filter: blur(10px);
    }
    
    .hero {
      min-height: 80vh;
      display: flex;
      align-items: center;
      position: relative;
      overflow: hidden;
    }
    
    .hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(45deg, rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.4));
      z-index: 1;
    }
    
    .hero-content {
      position: relative;
      z-index: 2;
    }
    
    .card {
      background-color: var(--card-bg);
      border: 1px solid var(--border-color);
      transition: transform 0.3s ease;
    }
    
    .card:hover {
      transform: translateY(-5px);
    }
    
    .btn-primary {
      background-color: var(--primary-color);
      border-color: var(--primary-color);
    }
    
    .social-icon {
      font-size: 1.5rem;
      margin: 0 10px;
      color: var(--text-color);
      transition: color 0.3s ease;
    }
    
    .social-icon:hover {
      color: var(--primary-color);
    }
    
    .project-card {
      position: relative;
      overflow: hidden;
      border-radius: 8px;
    }
    
    .project-card img {
      transition: transform 0.5s ease;
    }
    
    .project-card:hover img {
      transform: scale(1.05);
    }
    
    .project-overlay {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      background: linear-gradient(to top, rgba(0, 0, 0, 0.9), transparent);
      padding: 20px;
      transition: opacity 0.3s ease;
    }
    
    .project-title {
      color: white;
      margin-bottom: 8px;
    }
    
    .footer {
      background-color: rgba(0, 0, 0, 0.8);
      padding: 40px 0;
      margin-top: 80px;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
    <div class="container">
      <a class="navbar-brand" href="#"><%= portfolio.title %></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="#home">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#about">About</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#projects">Projects</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#contact">Contact</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <section id="home" class="hero">
    <div class="container hero-content">
      <div class="row">
        <div class="col-md-8">
          <h1 class="display-3 fw-bold"><%= portfolio.title %></h1>
          <p class="lead">Video Editor & Creator</p>
          <div class="mt-4">
            <a href="#projects" class="btn btn-primary btn-lg">View My Work</a>
            <a href="#contact" class="btn btn-outline-light btn-lg ms-2">Contact Me</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section id="about" class="py-5 mt-5">
    <div class="container">
      <div class="row">
        <div class="col-md-12 text-center mb-4">
          <h2 class="display-4">About Me</h2>
          <hr class="mx-auto" style="width: 80px; border-top: 3px solid var(--primary-color);">
        </div>
      </div>
      <div class="row">
        <div class="col-md-8 mx-auto">
          <p class="lead text-center">
            <%= portfolio.about || 'Professional video editor with a passion for storytelling through visual media.' %>
          </p>
        </div>
      </div>
      
      <% if (portfolio.skills && portfolio.skills.length > 0) { %>
        <div class="row mt-5">
          <div class="col-md-8 mx-auto">
            <h3 class="text-center mb-4">My Skills</h3>
            <div class="d-flex flex-wrap justify-content-center gap-2">
              <% portfolio.skills.forEach(skill => { %>
                <span class="badge bg-secondary p-2"><%= skill %></span>
              <% }) %>
            </div>
          </div>
        </div>
      <% } %>
    </div>
  </section>

  <section id="projects" class="py-5 mt-5">
    <div class="container">
      <div class="row">
        <div class="col-md-12 text-center mb-4">
          <h2 class="display-4">My Projects</h2>
          <hr class="mx-auto" style="width: 80px; border-top: 3px solid var(--primary-color);">
        </div>
      </div>
      
      <div class="row">
        <% if (projects.length === 0) { %>
          <div class="col-md-12 text-center">
            <p>No projects to display yet.</p>
          </div>
        <% } else { %>
          <% projects.forEach(project => { %>
            <div class="col-md-4 mb-4">
              <div class="project-card card h-100">
                <img src="<%= project.thumbnailUrl %>" class="card-img-top" alt="<%= project.title %>">
                <div class="project-overlay">
                  <h5 class="project-title"><%= project.title %></h5>
                  <p class="text-light"><%= project.category %></p>
                  <button class="btn btn-sm btn-primary" data-bs-toggle="modal" data-bs-target="#projectModal<%= project._id %>">
                    Watch Video
                  </button>
                </div>
              </div>
            </div>
            
            <!-- Project Modal -->
            <div class="modal fade" id="projectModal<%= project._id %>" tabindex="-1" aria-labelledby="projectModalLabel<%= project._id %>" aria-hidden="true">
              <div class="modal-dialog modal-lg">
                <div class="modal-content bg-dark text-light">
                  <div class="modal-header">
                    <h5 class="modal-title" id="projectModalLabel<%= project._id %>"><%= project.title %></h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                  </div>
                  <div class="modal-body">
                    <div class="ratio ratio-16x9 mb-3">
                      <video controls>
                        <source src="<%= project.videoUrl %>" type="video/mp4">
                        Your browser does not support the video tag.
                      </video>
                    </div>
                    <h6>Description:</h6>
                    <p><%= project.description %></p>
                    <% if (project.tags && project.tags.length > 0) { %>
                      <div class="mt-3">
                        <% project.tags.forEach(tag => { %>
                          <span class="badge bg-secondary me-1"><%= tag %></span>
                        <% }) %>
                      </div>
                    <% } %>
                  </div>
                </div>
              </div>
            </div>
          <% }) %>
        <% } %>
      </div>
    </div>
  </section>

  <section id="contact" class="py-5 mt-5">
    <div class="container">
      <div class="row">
        <div class="col-md-12 text-center mb-4">
          <h2 class="display-4">Get In Touch</h2>
          <hr class="mx-auto" style="width: 80px; border-top: 3px solid var(--primary-color);">
        </div>
      </div>
      
      <div class="row">
        <div class="col-md-6 mx-auto text-center">
          <% if (portfolio.contact.email) { %>
            <p><i class="fas fa-envelope me-2"></i> <%= portfolio.contact.email %></p>
          <% } %>
          
          <% if (portfolio.contact.phone) { %>
            <p><i class="fas fa-phone me-2"></i> <%= portfolio.contact.phone %></p>
          <% } %>
          
          <div class="social-links mt-4">
            <% if (portfolio.social.twitter) { %>
              <a href="<%= portfolio.social.twitter %>" target="_blank" class="social-icon"><i class="fab fa-twitter"></i></a>
            <% } %>
            
            <% if (portfolio.social.instagram) { %>
              <a href="<%= portfolio.social.instagram %>" target="_blank" class="social-icon"><i class="fab fa-instagram"></i></a>
            <% } %>
            
            <% if (portfolio.social.linkedin) { %>
              <a href="<%= portfolio.social.linkedin %>" target="_blank" class="social-icon"><i class="fab fa-linkedin"></i></a>
            <% } %>
            
            <% if (portfolio.social.youtube) { %>
              <a href="<%= portfolio.social.youtube %>" target="_blank" class="social-icon"><i class="fab fa-youtube"></i></a>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </section>

  <footer class="footer">
    <div class="container">
      <div class="row">
        <div class="col-md-12 text-center">
          <p>&copy; <%= new Date().getFullYear() %> <%= portfolio.title %>. All Rights Reserved.</p>
          <p class="small">Created with VideoFolio</p>
        </div>
      </div>
    </div>
  </footer>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
